E' possibile usare questi script per testare in modo automatico i casi d'uso già esistenti.

- creare un'ambiente virtuale 

- attivare l'ambiente:
> <env-dir>/bin/activate

- usare pip per installare le dipendenze:
> pip install -r requirements.txt

- lanciare lo script a seconda di quello che serve. Per esempio:
> python3 test01.py
